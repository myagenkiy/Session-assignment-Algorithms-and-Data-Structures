# Heroes Battle Project - Реализация алгоритмов

## 📁 Проект: Разработка алгоритмов для пошаговой стратегии

### 🎯 Обзор проекта
Данный проект посвящен реализации ключевых алгоритмов для пошаговой стратегии с боевой системой. Реализованы четыре основных алгоритма, отвечающих за игровую механику.

## 📊 Реализованные алгоритмы

### 1. GeneratePresetImpl - Генерация армии противника
Назначение: Формирование оптимального состава армии компьютера в рамках ограничений по очкам.

Логика алгоритма:
- Сортировка типов юнитов по эффективности (атака/стоимость, затем здоровье/стоимость)
- Жадный алгоритм добавления наиболее эффективных юнитов
- Соблюдение ограничений: максимум 11 юнитов каждого типа, бюджет 1500 очков
- Случайное размещение юнитов на поле (X: 0-2, Y: 0-20)

Алгоритмическая сложность:
O(K × N log N + M × L)

Где:
- K = 4 (количество типов юнитов)
- N = количество юнитов каждого типа (до 11)
- M = общее количество юнитов в армии
- L = поиск свободных координат (до 100 попыток)

Детализация сложности:
1. Сортировка типов юнитов: O(K log K) = O(4 log 4) = O(1)
2. Создание списка доступных юнитов: O(K × N) = O(4 × 11) = O(44) = O(1)
3. Жадный выбор: O(M) где M ≤ 1500/min_cost
4. Поиск координат: O(M × 100) в худшем случае
5. Итоговая сложность: O(M) или O(1) так как M ограничено

### 2. SimulateBattleImpl - Симуляция пошагового боя
Назначение: Проведение боя между армиями игрока и компьютера.

Логика алгоритма:
- Сбор всех живых юнитов из обеих армий
- Сортировка по убыванию значения атаки
- Поочередные ходы юнитов в порядке силы
- Обработка атак и обновление статуса юнитов
- Определение победителя при уничтожении одной из армий

Алгоритмическая сложность:
O(R × T log T)

Где:
- R = количество раундов боя
- T = общее количество юнитов (живых)

Детализация сложности:
1. Сбор живых юнитов: O(T)
2. Сортировка юнитов для каждого раунда: O(T log T)
3. Ходы юнитов: O(T) для каждого раунда
4. Метод атаки юнита: O(U) где U = количество юнитов противника
5. Итоговая сложность: O(R × T log T + R × T × U)
6. В худшем случае: O(R × T²) при U ≈ T

### 3. SuitableForAttackUnitsFinderImpl - Поиск доступных целей
Назначение: Определение юнитов, которые могут быть атакованы (не заблокированы своими).

Логика алгоритма:
- Для левой армии (компьютера): юниты не закрыты справа
- Для правой армии (игрока): юниты не закрыты слева
- Проверка наличия союзников на соседних позициях

Алгоритмическая сложность:
O(R × C)

Где:
- R = 3 (количество рядов - фиксировано)
- C = количество юнитов в ряду

Детализация сложности:
1. Проход по всем рядам: O(R) = O(3) = O(1)
2. Проход по юнитам в ряду: O(C) в каждом ряду
3. Проверка соседних рядов: O(C) для каждого юнита
4. Итоговая сложность: O(R × C²) в худшем случае
5. Но так как R фиксировано = 3: O(C²) или O(N) где N = общее количество юнитов

### 4. UnitTargetPathFinderImpl - Поиск кратчайшего пути
Назначение: Поиск оптимального маршрута между атакующим и целевым юнитом.

Логика алгоритма:
- Использование алгоритма Дейкстры с приоритетной очередью
- Учет препятствий (других живых юнитов)
- Движение в 8 направлениях (включая диагонали)
- Восстановление пути от цели к старту

Алгоритмическая сложность:
O(V log V + E)

Где:
- V = количество вершин графа = W × H = 27 × 21 = 567
- E = количество ребер ≈ 8 × V = 4536

Детализация сложности:
1. Инициализация массивов: O(V) = O(567) = O(1)
2. Алгоритм Дейкстры с PriorityQueue: O((V + E) log V)
3. В худшем случае: O((567 + 4536) log 567) ≈ O(5103 × 6.34) ≈ O(32350)
4. Итоговая сложность: O(V log V) = O(1) так как V фиксировано

## 🎯 Достигнутые цели

Соответствие требованиям:
1. Эффективность алгоритмов - все методы соответствуют заявленной O-сложности
2. Корректность логики - бой проходит по правилам, юниты умирают при нулевом здоровье
3. Оптимальность генерации - армия создается в рамках ограничений
4. Корректность поиска пути - находится кратчайший маршрут с обходом препятствий

Особенности реализации:
1. GeneratePreset - жадный алгоритм с приоритетом эффективности
2. SimulateBattle - учет порядка ходов и мгновенной смерти юнитов
3. SuitableForAttackUnitsFinder - проверка блокировки флангов
4. UnitTargetPathFinder - алгоритм Дейкстры с 8-направленным движением

## 🧪 Тестирование

Все алгоритмы протестированы на:
- Нормальных условиях (полные армии)
- Пограничных случаях (пустые армии, один юнит)
- Сложных сценариях (полное поле, блокированные цели)
- Производительности (время выполнения в пределах ожидаемого)

## 🔧 Технические детали

Используемые структуры данных:
1. PriorityQueue - для сортировки юнитов по атаке
2. HashMap - для подсчета типов юнитов
3. HashSet - для отслеживания ходивших юнитов
4. ArrayList - для хранения путей и списков

Особенности реализации:
- Конструкторы по умолчанию для всех классов
- Обработка null-значений
- Логирование ключевых событий
- Соответствие интерфейсам библиотеки игры

## 📝 Заключение

Все четыре алгоритма успешно реализованы и соответствуют требованиям проекта. Алгоритмическая сложность рассчитана корректно, производительность удовлетворительная для игровых условий. Реализация учитывает особенности игровой механики и обеспечивает корректную работу всех систем пошаговой стратегии.
